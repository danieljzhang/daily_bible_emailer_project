Shared account guidance

Put cross-account or global resources in `shared/`, for example:
- SSO / Identity Center configuration
- Organization-level IAM
- centralized logging / monitoring
- terraform backend bucket (optional)
